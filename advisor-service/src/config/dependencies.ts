import { advisorRepository} from '../libs/app/repository/index'
import {advisor_Usecase} from '../libs/usecase';

const useCase:any = {
   advisor_Usecase
};
const repository:any={advisorRepository}
export default {useCase,repository }