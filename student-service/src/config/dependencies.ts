import { studentRepository } from '../libs/app/repository/index'
import {student_Usecase } from '../libs/usecase';

const useCase:any = {
   student_Usecase
};
const repository:any={studentRepository}
export default {useCase,repository }